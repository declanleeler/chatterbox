import { Box, Paper } from '@mui/material';
import { FC } from 'react';

const ChatHistory: FC = () => {
  return (
    <Paper sx={{ minHeight: '100vh', borderRight: 'solid red' }}>
      chat history nav bar here
    </Paper>
  );
};

export default ChatHistory;
