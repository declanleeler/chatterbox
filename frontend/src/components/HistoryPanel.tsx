import { Paper } from '@mui/material';
import { FC } from 'react';

const HistoryPanel: FC = () => {
  return <Paper sx={{ minHeight: '100vh' }}>chat history nav bar here</Paper>;
};

export default HistoryPanel;
